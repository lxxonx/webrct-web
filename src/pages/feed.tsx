import React, { ReactElement } from "react";
import NormalLayout from "../components/NormalLayout";

interface Props {}

function Feed({}: Props): ReactElement {
  return <NormalLayout>feed</NormalLayout>;
}

export default Feed;
